import React from "react";

export default function AdminSettings() {
  return (
    <div style={{ padding: 24 }}>
      <h1>시스템 설정 (SUPER_ADMIN 전용)</h1>
      <p>역할/권한, 전역 설정 등을 관리하는 영역입니다. (더미 UI)</p>
    </div>
  );
}
